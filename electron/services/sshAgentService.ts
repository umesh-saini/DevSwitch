/**
 * Re-export of the shared core SSH agent service.
 * Shared by the desktop app and the `devswitch` CLI.
 */
export {
  SSHAgentService,
  sshAgentService,
} from "../../core/services/sshAgentService.ts";
